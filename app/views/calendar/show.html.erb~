<h1>Calendar</h1>

<div class="row">
  <div class="col-md-12 text-center">
    <div class="well controls">
    <H2> 
      <%= link_to calendar_path(date: @date - 1.month), class: "btn btn-default" do %>
        <i class="glyphicon glyphicon-backward"></i>
      <% end %>
      <%= "#{@date.strftime("%B")} #{@date.year}" %>  
      <%= link_to calendar_path(date: @date + 1.month), class: "btn btn-default" do %>
        <i class="glyphicon glyphicon-forward"></i>
      <% end %>
    </H2>  
    </div>
  </div>
</div>
<div class="row">
  <div class="col-md-12">
    <%= calendar @date do |date| %>
      <%= date.day %>  
      
      <% if @gnotes_by_date[date] %>
        <ul>
          <% @gnotes_by_date[date].each do |garden_note| %>
            <li><%= link_to garden_note.g_note, garden_note %></li>
          <% end %>
        </ul>
      <% end %>
      
      
    <% end %>
  </div>
</div>

<%= link_to 'New G note', new_g_note_path %>
